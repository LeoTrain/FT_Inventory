<UserControl x:Class="FT_Inventory.MVVM.View.OrderItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FT_Inventory.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Background="{StaticResource BlackBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Order Item ID:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.OrderItemId}" Background="White" Width="240"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Product ID:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.ProductId}" Background="White" Width="240"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Quantity:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.Quantity}" Background="White" Width="240"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Total Price:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.TotalPrice, StringFormat=\{0:C\}}" Background="White" Width="240"/>
            </StackPanel>

            <TextBlock Text="Product Quick Tips:" FontSize="20" Grid.Row="4" Padding="10" />

            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Product Name:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.Product.Name}" Background="White" Width="240"/>
            </StackPanel>

            <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Product Price:" Width="100"/>
                <TextBlock Text="{Binding CurrentOrderItem.Product.Price, StringFormat=\{0:C\}}" Background="White" Width="240"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="100" Height="30" Margin="10"
                        Command="{Binding DataContext.GoBackCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
                <Button Content="Save" Width="100" Height="30" Margin="10"
                        Command="{Binding DataContext.SaveOrderitemCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                        CommandParameter="{Binding CurrentOrderItem}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
